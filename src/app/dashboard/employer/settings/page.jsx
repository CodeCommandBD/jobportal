import SettingsPanel from "@/Components/dashboard/SettingsPanel";

export const metadata = {
  title: "Settings - Employer Dashboard",
};

export default function EmployerSettings() {
  return <SettingsPanel role="Employer" />;
}
