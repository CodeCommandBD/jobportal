import SettingsPanel from "@/Components/dashboard/SettingsPanel";

export const metadata = {
  title: "Settings - Candidate Dashboard",
};

export default function CandidateSettings() {
  return <SettingsPanel role="Candidate" />;
}
